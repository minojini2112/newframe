# FillFrame frontend

Next.js mission console for ISRO PS12 â€” talks to the FastAPI backend (api_server.py).

Tabs: Overview, Interpolation, Motion / AMV, Batch Validate, Training.

Run:
  cd Frontend
  npm install
  npm run dev
